<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <Toolbar
        android:id="@+id/toolbar_dashboard"
        android:layout_width="match_parent"
        android:layout_height="400px"
        android:contentInsetStart="0dp"
        android:background="#895EDA"
        app:layout_constraintTop_toTopOf="parent">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="50px">

            <ImageButton
                android:id="@+id/drawer_icon_id"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                android:background="@android:color/transparent"
                android:src="@drawable/option_menu_icon"
                app:layout_constraintStart_toStartOf="@id/text_view_hi_user"
                app:layout_constraintTop_toTopOf="parent" />

            <TextView
                android:id="@+id/text_view_hi_user"
                android:layout_width="50dp"
                android:layout_height="20dp"
                android:layout_marginStart="30dp"
                android:layout_marginTop="10dp"
                android:text="Hi User!"
                android:textColor="@color/black"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/drawer_icon_id" />

            <TextView
                android:fontFamily="sans-serif-light"
                android:layout_width="200sp"
                android:layout_height="40sp"
                android:text="Your courses"
                android:textColor="@color/black"
                android:textSize="28sp"
                app:layout_constraintStart_toStartOf="@id/text_view_hi_user"
                app:layout_constraintTop_toBottomOf="@id/text_view_hi_user"

                />

        </androidx.constraintlayout.widget.ConstraintLayout>
    </Toolbar>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/Recycler_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:background="#8055A4CF"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@id/toolbar_dashboard"

        tools:listitem="@layout/dashboard_item" />

</androidx.constraintlayout.widget.ConstraintLayout>

